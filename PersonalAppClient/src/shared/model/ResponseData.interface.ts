export interface ResponseData {
  isSuccess?: boolean,
  result?: any,
  errorMessages?: string
}

export interface ResponseDatas {
  isSuccess?: boolean,
  datas?: any,
  errorMessages?: string,
  totalItem?: number,
}
